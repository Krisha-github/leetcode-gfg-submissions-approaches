                for (int num = 2; num>=0; num--) {                                for (int canbuy = 0; canbuy <= 1; canbuy++) {                    int profit = 0;                    if (canbuy) {                        int buys = 0;                        if (num < 2)                            buys = dp[i+1][0][num] - prices[i];                        profit = max(buys, dp[i+1][1][num]);                    }                     else{                        int sells=0;                        if(num<2){                            sells=dp[i+1][1][num+1] + prices[i];                        }        for (int i = n - 1; i >= 0; i--) {            n + 1, vector<vector<int>>(2, vector<int>(3, 0)));        vector<vector<vector<int>>> dp(                        profit =                            max(sells,                                dp[i+1][0][num]);                    }                                        dp[i][canbuy][num] = profit;                }            }        }        return dp[0][1][2];    }};