                        if (num < k)                            buys = dp[i+1][0][num] - prices[i];                        profit = max(buys, dp[i+1][1][num]);                    }                     if (canbuy) {                        int buys = 0;                                        int profit = 0;                for (int num = 0; num<=k; num++) {            for (int canbuy = 0; canbuy <= 1; canbuy++) {        for (int i = n - 1; i >= 0; i--) {//return f(0,1,0,prices,dp);            n + 1, vector<vector<int>>(2, vector<int>(k+2, 0)));        vector<vector<vector<int>>> dp(        int n = prices.size();